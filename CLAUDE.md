# CLAUDE.md

## 1. 主要目标

开发销售管理系统（小程序 + Web 端），实现：
- 商品管理与销售
- 客户资料管理
- 销售人员管理
- 销售数据统计

## 2. 目录结构

```
SaleManagerSys
├── Backend/                # 后端项目 (SpringBoot)
├── Fronted/                # Web 管理端 (Vue3)
├── Uniapp-User/            # 用户端小程序 (uni-app)
├── Uniapp-Admin/           # 管理端小程序 (uni-app)
├── docs/                   # 项目文档
├── sql/                    # 数据库脚本
└── .claude/
    └── rules/              # 编码规范
```

## 3. Git 版本管理

### 分支策略

| 分支类型 | 命名规范 | 说明 |
|----------|----------|------|
| **主分支** | `main` | 最终稳定版本，所有端合并后同步 |
| **端分支** | `Backend` / `Fronted` / `Uniapp-User` / `Uniapp-Admin` | 各端开发主分支 |
| **功能分支** | `{module}-{name}` | 功能开发分支，如 `product-zhangsan` |

### 分支流程

```
┌─────────────────────────────────────────────────────────────┐
│                          main                              │
│            (最终稳定版，所有端开发完成后再同步)               │
└─────────────────────────────────────────────────────────────┘
          ↑            ↑            ↑            ↑
     合并Backend  合并Fronted 合并Uniapp-User 合并Uniapp-Admin
          ↑            ↑            ↑            ↑
┌─────────────────┐┌─────────────────┐┌─────────────────┐┌─────────────────┐
│    Backend      ││    Fronted      ││  Uniapp-User    ││  Uniapp-Admin   │
│  (后端开发主分支) ││ (Web管理端主分支) ││ (用户端小程序主分支) ││ (管理端小程序主分支) │
└─────────────────┘└─────────────────┘└─────────────────┘└─────────────────┘
          ↑            ↑            ↑            ↑
      合并功能分支  合并功能分支  合并功能分支  合并功能分支
          ↑            ↑            ↑            ↑
┌─────────────────┐┌─────────────────┐┌─────────────────┐┌─────────────────┐
│product-zhangsan ││product-zhangsan ││product-zhangsan ││product-zhangsan │
│sale-zhangsan    ││sale-zhangsan    ││sale-zhangsan   ││sale-zhangsan    │
└─────────────────┘└─────────────────┘└─────────────────┘└─────────────────┘
```

### 开发流程

1. **创建功能分支**: 从对应端分支创建 `module-name` 格式的功能分支
2. **开发提交**: 在功能分支上开发，功能完成后提交 PR/MR
3. **合并到端分支**: 代码审核后合并到 `Backend` / `Fronted` / `Uniapp-User` / `Uniapp-Admin`
4. **同步到 main**: 端分支稳定后，合并到 `main`

### 分支命名示例

```
# 后端
Backend
Backend-product-zhangsan      # 张三开发商品模块
Backend-sale-zhangsan        # 张三开发销售模块

# Web管理端
Fronted
Fronted-product-zhangsan     # 张三开发商品模块
Fronted-customer-lisi        # 李四开发客户模块

# 用户端小程序
Uniapp-User
Uniapp-User-product-zhangsan # 张三开发商品模块

# 管理端小程序
Uniapp-Admin
Uniapp-Admin-product-zhangsan # 张三开发商品模块
```

## 4. 编码规范

遵循 `.claude/rules/` 下的规范：
- `coding-style.md` - 代码组织、命名、方法长度
- `frontend-style.md` - 前端 UI 设计规范
- `security.md` - 安全规则
- `testing.md` - 测试规范

## 5. 包结构

```
com.salemanager.modules.{module}/
├── controller/     # Controller
├── service/        # Service 接口
├── impl/           # Service 实现
├── mapper/         # Mapper 接口
├── model/          # 实体类
├── dto/            # 数据传输对象
├── vo/             # 视图对象
├── param/          # 请求参数
└── enums/          # 枚举类
```

## 6. 模块划分

| 模块 | 包名 | 说明 |
|------|------|------|
| 员工管理 | `modules.ums` | 员工、角色、权限、菜单路由 |
| 商品管理 | `modules.product` | 商品类别、商品信息、SN码 |
| 客户管理 | `modules.customer` | 客户资料、会员 |
| 销售管理 | `modules.sale` | 销售订单、明细 |
| 统计管理 | `modules.statistics` | 数据统计、报表 |

## 7. 四端架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              后端 API (SpringBoot)                          │
│                    统一认证 · 动态路由 · 权限控制 · 数据服务                  │
└─────────────────────────────────────────────────────────────────────────────┘
              ↑                       ↑                       ↑                       ↑
              │                       │                       │                       │
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│   Uniapp-Admin  │       │     Fronted     │       │   Uniapp-User   │       │    Backend      │
│  (管理端小程序)   │       │   (Web管理端)   │       │  (用户端小程序)  │       │   (后端API)     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│• SN扫码录入      │       │• 商品批量管理    │       │• 商品浏览        │       │• 业务逻辑       │
│• SN扫码编辑      │       │• CSV导入导出     │       │• 购物车          │       │• 数据持久化     │
│• 商品查询       │       │• 手动SN录入     │       │• 下单支付        │       │• 认证授权       │
│• 操作记录       │       │• 订单管理       │       │• 会员中心        │       │• 权限控制       │
│• 权限验证       │       │• 客户管理       │       │• 积分兑换        │       │• 统计报表       │
│                 │       │• 数据统计       │       │• 订单查询        │       │                 │
│                 │       │• 动态路由       │       │                 │       │                 │
│                 │       │• 权限控制       │       │                 │       │                 │
└─────────────────┘       └─────────────────┘       └─────────────────┘       └─────────────────┘
```

## 8. 权限控制设计

### 8.1 数据库表

| 表名 | 说明 |
|------|------|
| `menu` | 菜单/路由表，存储前端路由和按钮权限 |
| `role` | 角色表 |
| `role_menu` | 角色菜单关联表 |
| `admin_user` | 员工用户表 |

### 8.2 菜单类型

| type | 说明 |
|------|------|
| 1 | 菜单（前端路由） |
| 2 | 按钮权限 |
| 3 | 接口权限 |

### 8.3 权限标识示例

```
product:view    - 查看商品
product:add     - 新增商品
product:edit    - 编辑商品
product:delete  - 删除商品
product:import  - 导入商品
product:export  - 导出商品
order:view      - 查看订单
order:edit      - 编辑订单
system:user     - 员工管理
system:role     - 角色管理
```

### 8.4 前端实现

| 功能 | 实现方式 |
|------|----------|
| 动态路由 | 后端返回路由列表，前端动态注册 |
| 权限数组 | 后端返回 `permissions[]`，前端存储 |
| 路由守卫 | `router.beforeEach` 校验权限 |
| 按钮控制 | `v-permission` 指令控制显示 |

### 8.5 API接口

```
POST /api/admin/auth/login          - 员工登录
GET  /api/admin/auth/routes         - 获取路由+权限数组
POST /api/admin/auth/check          - 权限校验
```

### 8.6 预设角色模板

| 角色 | 权限范围 |
|------|----------|
| **超级管理员** | 所有权限（不可删除） |
| **运营主管** | 商品、订单、客户、数据统计管理 |
| **录入员** | 商品新增、SN码查询、操作记录 |

### 8.7 单独权限管理

- 新用户默认继承预设角色权限
- 可针对单个用户增删权限
- 权限叠加：额外授予权限在角色权限基础上叠加
- 权限排除：可单独禁用某权限

### 8.8 权限计算公式

```
有效权限 = 角色权限 ∪ 额外权限 - 禁用权限
```

### 8.9 路由单独管理

- 新用户默认继承预设角色的路由
- 可针对单个用户增删路由
- 路由叠加：额外授予的路由在角色路由基础上叠加
- 路由排除：可单独禁用某路由

### 8.10 路由计算公式

```
用户可见路由 = 角色路由 ∪ 额外路由 - 禁用路由
```

## 9. 批量操作设计（Web管理端）

### 9.1 CSV导入导出

| 操作 | 说明 |
|------|------|
| 导出模板 | 下载标准CSV表头 |
| 导入流程 | 上传CSV → 预览 → 确认 → 结果报告 |
| 导出数据 | 导出全部/导出选中 |

### 9.2 批量编辑

支持批量修改：分类、价格、状态、描述

## 10. 小程序双端设计

### 10.1 用户端小程序

面向客户，主打会员商城。

| 模块 | 功能 | 详情 |
|------|------|------|
| 首页 | 商品展示、分类入口、搜索 | Banner轮播、热销推荐、会员卡片 |
| 商品 | 分类浏览、详情、价格、库存 | 图片、描述、加购 |
| 购物车 | 添加商品、数量管理、结算 | 全选、计算总价 |
| 订单 | 创建、支付、列表、详情、退款 | 微信支付/余额支付 |
| 会员中心 | 余额充值、积分商城/兑换 | 积分明细、个人资料 |

**页面结构**：首页、商品列表、商品详情、购物车、确认订单、支付页、订单列表、订单详情、会员中心、余额充值、积分商城、个人资料（12个页面）

### 10.2 管理端小程序

面向管理人员，轻量商品录入平台。

| 模块 | 功能 | 详情 |
|------|------|------|
| 登录 | 手机号+验证码 | 后台审核开通 |
| 首页 | 快捷入口、今日操作 | 扫码、录入、查询、记录 |
| 扫码 | 扫码识别、新增/编辑商品 | 扫码→表单→保存 |
| SN查询 | 输入/扫码查询 | 快速查找商品信息 |
| 操作记录 | 操作列表 | 时间、类型、SN码、商品 |

**SN码操作逻辑**：
```
扫描SN码 → 查询是否存在
    ├── 存在 → 进入编辑模式
    └── 不存在 → 进入新增模式
```

**权限验证**：手机号+验证码登录，后台审核开通 + 操作日志追溯

**页面结构**：登录页、首页、扫码页、商品列表、商品详情/编辑、SN查询、操作记录、我的（8个页面）

## 7. 研发过程归档

每个**大的推进步骤**都需要记录归档，存放在 `doc/` 目录下。

### 归档要求

| 要求 | 说明 |
|------|------|
| **多文件记录** | 每个大里程碑独立一个文件 |
| **命名规范** | `doc/YYYY-MM-DD-里程碑名称.md` |
| **内容要素** | 技术决策、目录结构变化、组件清单、UI规范、问题解决 |
| **CLAUDE.md 摘要** | 仅记录简短摘要，指向 doc/ 详细文档 |

### 归档内容模板

```markdown
# YYYY-MM-DD 里程碑名称

## 背景
为什么做这个决策/改动

## 技术决策
- 选型理由
- 替代方案对比

## 目录结构
当前最新的项目结构

## 组件/模块清单
新增或改动的组件列表

## UI 设计规范
颜色、断点、布局等

## 问题与解决
遇到的问题及解决方案

## 下一步
后续计划
```

### 归档清单

- 【2026-03-27】项目初始化，创建基础目录结构 → `doc/2026-03-27-项目初始化.md`
- 【2026-03-27】前端项目搭建 → `doc/2026-03-27-前端项目搭建.md`
- 【2026-03-27】组件封装 → `doc/2026-03-27-组件封装.md`
- 【2026-03-27】Arco Design + Tailwind 重构 → `doc/2026-03-27-ArcoDesign重构.md`
- 【2026-03-27】需求详细设计 → `doc/2026-03-27-需求详细设计.md`
