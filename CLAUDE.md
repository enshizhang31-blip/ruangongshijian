# CLAUDE.md

## 1. 主要目标

开发销售管理系统（小程序 + Web 端），实现：
- 商品管理与销售
- 客户资料管理
- 销售人员管理
- 销售数据统计

## 2. 目录结构

```
SaleManagerSys
├── Backend/                # 后端项目 (SpringBoot)
├── Fronted/                # Web 管理端 (Vue3)
│                          # 包含：商品管理、SN码录入、订单管理、客户管理等
├── Uniapp/                 # 用户端小程序 (uni-app)
├── doc/                    # 项目文档
├── sql/                    # 数据库脚本
└── .claude/
    └── rules/              # 编码规范
```

## 3. Git 版本管理

### 分支策略

| 分支类型 | 命名规范 | 说明 |
|----------|----------|------|
| **主分支** | `main` | 最终稳定版本，所有端合并后同步 |
| **端分支** | `Backend` / `Fronted` / `Uniapp` | 各端开发主分支 |
| **功能分支** | `{module}-{name}` | 功能开发分支，如 `product-zhangsan` |

### 分支流程

```
┌─────────────────────────────────────────────────────────────┐
│                          main                              │
│            (最终稳定版，所有端开发完成后再同步)               │
└─────────────────────────────────────────────────────────────┘
          ↑            ↑            ↑
     合并Backend  合并Fronted  合并Uniapp
          ↑            ↑            ↑
┌─────────────────┐┌─────────────────┐┌─────────────────┐
│    Backend      ││    Fronted      ││     Uniapp      │
│  (后端开发主分支) ││ (Web管理端主分支) ││ (用户端小程序主分支) │
└─────────────────┘└─────────────────┘└─────────────────┘
          ↑            ↑            ↑
      合并功能分支  合并功能分支  合并功能分支
          ↑            ↑            ↑
┌─────────────────┐┌─────────────────┐┌─────────────────┐
│product-zhangsan ││product-zhangsan ││product-zhangsan │
│sale-zhangsan    ││sale-zhangsan    ││sale-zhangsan   │
└─────────────────┘└─────────────────┘└─────────────────┘
```

### 开发流程

1. **创建功能分支**: 从对应端分支创建 `module-name` 格式的功能分支
2. **开发提交**: 在功能分支上开发，功能完成后提交 PR/MR
3. **合并到端分支**: 代码审核后合并到 `Backend` / `Fronted` / `Uniapp`
4. **同步到 main**: 端分支稳定后，合并到 `main`

### 分支命名示例

```
# 后端
Backend
Backend-product-zhangsan      # 张三开发商品模块
Backend-sale-zhangsan        # 张三开发销售模块

# Web管理端
Fronted
Fronted-product-zhangsan     # 张三开发商品模块
Fronted-customer-lisi        # 李四开发客户模块

# 用户端小程序
Uniapp
Uniapp-product-zhangsan # 张三开发商品模块
```

## 4. 编码规范

遵循 `.claude/rules/` 下的规范：
- `coding-style.md` - 代码组织、命名、方法长度
- `frontend-style.md` - 前端 UI 设计规范
- `security.md` - 安全规则
- `testing.md` - 测试规范

## 5. 包结构

```
com.salemanager.modules.{module}/
├── controller/     # Controller
├── service/        # Service 接口
├── impl/           # Service 实现
├── mapper/         # Mapper 接口
├── model/          # 实体类
├── dto/            # 数据传输对象
├── vo/             # 视图对象
├── param/          # 请求参数
└── enums/          # 枚举类
```

## 6. 模块划分

| 模块 | 包名 | 说明 |
|------|------|------|
| 员工管理 | `modules.ums` | 员工、角色、权限、菜单路由 |
| 商品管理 | `modules.product` | 商品类别、商品信息 |
| SN码管理 | `modules.sn` | SN码录入/查询/操作记录 |
| 客户管理 | `modules.customer` | 客户资料、会员 |
| 销售管理 | `modules.sale` | 销售订单、明细 |
| 统计管理 | `modules.statistics` | 数据统计、报表 |

## 7. 三端架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              后端 API (SpringBoot)                          │
│                    统一认证 · 动态路由 · 权限控制 · 数据服务                  │
└─────────────────────────────────────────────────────────────────────────────┘
              ↑                       ↑                       ↑
              │                       │                       │
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│     Fronted     │       │     Uniapp      │       │    Backend      │
│   (Web管理端)   │       │  (用户端小程序)   │       │   (后端API)     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│• 商品批量管理    │       │• 商品浏览        │       │• 业务逻辑       │
│• CSV导入导出     │       │• 购物车          │       │• 数据持久化     │
│• SN码录入/查询  │       │• 下单支付        │       │• 认证授权       │
│• 订单管理       │       │• 会员中心        │       │• 权限控制       │
│• 客户管理       │       │• 积分兑换        │       │• 统计报表       │
│• 数据统计       │       │• 订单查询        │       │                 │
│• 动态路由       │       │                 │       │                 │
│• 权限控制       │       │                 │       │                 │
└─────────────────┘       └─────────────────┘       └─────────────────┘
```

## 8. 权限控制设计

### 8.1 数据库表

| 表名 | 说明 |
|------|------|
| `menu` | 菜单/路由表，存储前端路由和按钮权限 |
| `role` | 角色表 |
| `role_menu` | 角色菜单关联表 |
| `admin_user` | 员工用户表 |

### 8.2 菜单类型

| type | 说明 |
|------|------|
| 1 | 菜单（前端路由） |
| 2 | 按钮权限 |
| 3 | 接口权限 |

### 8.3 权限标识示例（统一规范）

```
# 商品模块 - SPU
spu:view       - 查看商品列表
spu:add        - 新增商品
spu:edit       - 编辑商品
spu:delete     - 删除商品
spu:import     - 导入商品
spu:export     - 导出商品
spu:status     - 修改商品状态

# 商品模块 - SKU
sku:view       - 查看SKU列表
sku:add        - 新增SKU
sku:edit       - 编辑SKU
sku:delete     - 删除SKU

# 商品模块 - 分类
category:view  - 查看分类
category:add   - 新增分类
category:edit  - 编辑分类

# 商品模块 - 规格
spec:view      - 查看规格
spec:add       - 新增规格

# SN码模块
sn:view        - 查看SN码
sn:add         - 录入SN码
sn:import      - 批量导入
sn:query       - SN码查询

# 订单模块
order:view     - 查看订单
order:edit     - 编辑订单

# 系统模块
system:user    - 员工管理
system:role    - 角色管理
```

### 8.4 前端实现

| 功能 | 实现方式 |
|------|----------|
| 动态路由 | 后端返回路由列表，前端动态注册 |
| 权限数组 | 后端返回 `permissions[]`，前端存储 |
| 路由守卫 | `router.beforeEach` 校验权限 |
| 按钮控制 | `v-permission` 指令控制显示 |

### 8.5 API接口

```
POST /api/admin/auth/login          - 员工登录
GET  /api/admin/auth/routes         - 获取路由+权限数组
POST /api/admin/auth/check          - 权限校验
```

### 8.6 预设角色模板

| 角色 | 权限范围 |
|------|----------|
| **超级管理员** | 所有权限（不可删除） |
| **运营主管** | 商品、订单、客户、数据统计管理 |
| **录入员** | 商品新增、SN码查询、操作记录 |

### 8.7 单独权限管理

- 新用户默认继承预设角色权限
- 可针对单个用户增删权限
- 权限叠加：额外授予权限在角色权限基础上叠加
- 权限排除：可单独禁用某权限

### 8.8 权限计算公式

```
有效权限 = 角色权限 ∪ 额外权限 - 禁用权限
```

### 8.9 路由单独管理

- 新用户默认继承预设角色的路由
- 可针对单个用户增删路由
- 路由叠加：额外授予的路由在角色路由基础上叠加
- 路由排除：可单独禁用某路由

### 8.10 路由计算公式

```
用户可见路由 = 角色路由 ∪ 额外路由 - 禁用路由
```

## 9. 批量操作设计（Web管理端）

### 9.1 CSV导入导出

| 操作 | 说明 |
|------|------|
| 导出模板 | 下载标准CSV表头 |
| 导入流程 | 上传CSV → 预览 → 确认 → 结果报告 |
| 导出数据 | 导出全部/导出选中 |

### 9.2 批量编辑

支持批量修改：分类、价格、状态、描述

## 10. 小程序设计

### 10.1 用户端小程序

面向客户，主打会员商城。

| 模块 | 功能 | 详情 |
|------|------|------|
| 首页 | 商品展示、分类入口、搜索 | Banner轮播、热销推荐、会员卡片 |
| 商品 | 分类浏览、详情、价格、库存 | 图片、描述、加购 |
| 购物车 | 添加商品、数量管理、结算 | 全选、计算总价 |
| 订单 | 创建、支付、列表、详情、退款 | 微信支付/余额支付 |
| 会员中心 | 余额充值、积分商城/兑换 | 积分明细、个人资料 |

**页面结构**：首页、商品列表、商品详情、购物车、确认订单、支付页、订单列表、订单详情、会员中心、余额充值、积分商城、个人资料（12个页面）

## 12. 文档同步规则

### 12.1 核心原则

**每次修改文档时，必须检查并同步相关文档**，避免文档之间不一致。

### 12.2 文档依赖关系

| 修改内容 | 需要同步的文档 |
|----------|---------------|
| 修改数据库表结构 | 数据库设计.md、需求详细设计.md、sql/init.sql |
| 修改API接口 | API接口文档、需求详细设计.md |
| 修改功能设计 | 需求详细设计.md、数据库设计.md |
| 修改页面结构 | 需求详细设计.md |

### 12.3 文档清单

| 文档 | 说明 | 状态 |
|------|------|------|
| 需求详细设计.md | 功能需求 | ✅ 已完成 |
| 数据库设计.md | 表结构设计 | ✅ 已完成 |
| 国际化方案.md | 国际化设计（MySQL + MongoDB） | ✅ 已完成 |
| 文档开发计划.md | 任务跟踪 | ✅ 已完成 |
| sql/init.sql | 建表脚本 | ✅ 已完成 |
| API接口文档.md | 接口定义 | ⏳ 待创建 |

### 12.4 归档清单（持续更新）

- 【2026-03-27】项目初始化 → `doc/2026-03-27-项目初始化.md`
- 【2026-03-27】需求详细设计 → `doc/2026-03-27-需求详细设计.md`
- 【2026-03-28】数据库设计 → `doc/2026-03-28-数据库设计.md`
- 【2026-03-29】文档开发计划 → `doc/2026-03-29-文档开发计划.md`
- 【2026-03-29】国际化方案 → `doc/2026-03-29-国际化方案.md`
- 【2026-03-29】数据库初始化脚本 → `sql/init.sql`
