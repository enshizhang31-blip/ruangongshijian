spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sale_manager?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: salemanager
    password: Sm@MySQL2026User
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    mongodb:
      uri: mongodb://salemanager:Sm%40Mongo2026Admin@localhost:27017/sale_manager_i18n?authSource=admin
    redis:
      host: localhost
      port: 6379
      password: Sm@Redis2026Cache

# 可在本地环境覆盖 JWT（若需）
jwt:
  secret: sale-manager-jwt-secret-key-2026
  expiration: 86400000

# 本地开发时允许较小的 page size
sale-manager:
  pagination:
    max-page-size: 100
